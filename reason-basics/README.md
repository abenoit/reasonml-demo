# Basic Reason

## Run project

```
# Build only
npm run build

# Build + watch
npm run start

# In another terminal
node src/Demo.bs.js
```

# Editor

If you use `vscode`, Press `Windows + Shift + B` it will build automatically
